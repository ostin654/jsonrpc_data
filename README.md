# Requirements

- PHP 7.4
- Database (MySQL or Postgres)

# Install

- `composer install`
- `cp .env.dist .env`
- Edit .env and configure database connection

# Start

`symfony serve --no-tls --port=8001`
